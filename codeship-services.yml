lergo-build:
  cached: false
  # cached: false # commenting out for development. should not be commented when live
  build:
    image: lergo-build
    dockerfile: Dockerfile
  encrypted_env_file: env.encrypted
